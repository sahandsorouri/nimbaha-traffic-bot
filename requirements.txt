# Claude Usage Bar — menu bar app dependencies
rumps==0.4.0
browser-cookie3==0.19.1
curl_cffi==0.7.4
pyobjc-core>=10.0
pyobjc-framework-Cocoa>=10.0
# Optional: only used by login_once.py (standalone login helper)
# playwright==1.48.0
